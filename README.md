# Slack UI Sandbox

Static Slack-style workspace prototype focused on the message thread and composer experience.

## Run locally

Open `index.html` in a browser, or serve the directory with any static file server.

## Validate

```bash
npm run validate
```
