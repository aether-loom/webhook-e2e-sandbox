# Slack UI Shell

A static Slack-style app shell focused on channel sidebar behavior: active channel highlighting, unread counts, mention badges, and muted channel states.

## Run

Open `index.html` in a browser.

## Validate

```bash
npm test
```
