# Slack UI Sidebar

A lightweight Slack-style app shell focused on channel sidebar behavior: active-channel selection, unread badges, muted channels, and workspace unread totals.

## Run validation

```bash
npm test
```

Open `index.html` in a browser to view the static UI.
