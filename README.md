# Slack UI Sidebar

A lightweight Slack-style app shell focused on channel sidebar states, unread badges, and active-channel behavior.

## Commands

- `npm test` - run sidebar state tests
- `npm run validate` - run the validation suite
- `npm start` - serve the static UI on port 4173
